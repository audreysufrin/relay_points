<% if admin_signed_in? %>
<p>Hello!<%= current_admin.email%></p>
<% end %>

<div class="container">
  <div class="row">
    <div>
      <%= link_to "Créer un point relais", new_relay_point_path, class: "btn btn-flat" %>
      <% @relay_points.each do |relay_point| %>
        <div class="col-xs-4">

          <%= link_to relay_point_path(relay_point) do %>
            <div class="card-product">
              <img src=<%= relay_point.image_shopify %>>
              <div class="card-product-infos">
                <h2><strong><%= relay_point.name %></strong></h2>
                <p><%= relay_point.address %>,<%= relay_point.hours %>, <%= relay_point.phone_number %></p>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
